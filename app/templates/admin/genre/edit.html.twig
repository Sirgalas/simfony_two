{% extends 'base_admin.html.twig' %}
{% block title %}{{ "Edit genre"|trans }} {{ genre.title}} {% endblock %}
{% block head  %}{{ "Edit genre"|trans }} {{ genre.title}} {% endblock %}
{% block breadcrumbs %}
    <li class="breadcrumb-item">
        <a href="{{ path('admin_genre_index') }}">{{ 'Genre'|trans }}</a>
    </li>
    <li class="breadcrumb-item active">{{ "Edit"|trans }}</li>
{% endblock %}
{% block body %}
<div class="col-md-12">
    <!-- general form elements -->
    <div class="card card-primary">
        <div class="card-header">
            <h3 class="card-title">{{ "Edit"|trans }}</h3>
            <div class="card-tools"> {{ include('admin/genre/_delete_form.html.twig') }} </div>
        </div>
        {{ include('admin/genre/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
</div>

{% endblock %}
