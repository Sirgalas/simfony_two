{% extends 'base_admin.html.twig' %}

{% block title %}Edit Author {{ author.fullName}} {% endblock %}
{% block head  %}Edit Author {{ author.fullName}}{% endblock %}
{% block breadcrumbs %}
    <li class="breadcrumb-item">
        <a href="{{ path('admin_author_index') }}">{{ 'Author'|trans }}</a>
    </li>
    <li class="breadcrumb-item active">{{ "Edit"|trans }}</li>
{% endblock %}
{% block body %}
<div class="col-md-12">
    <!-- general form elements -->
    <div class="card card-primary">
        <div class="card-header">
            <h3 class="card-title">{{ "Edit"|trans }}</h3>
            <div class="card-tools"> {{ include('admin/author/_delete_form.html.twig') }} </div>
        </div>
            {{ include('admin/author/_form.html.twig', {'button_label': 'Редактировать'}) }}
    </div>
</div>


{% endblock %}
