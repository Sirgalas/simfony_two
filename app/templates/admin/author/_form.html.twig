{{ form_start(form) }}
    <div class="card-body">
        <div class="form-group">
            {{ form_label(form.name) }}
            {{ form_errors(form.name) }}
            {{ form_widget(form.name,{'attr':{'class':'form-control'}}) }}
            {{ form_help(form.name) }}
        </div>
        <div class="form-group">
            {{ form_label(form.patronic) }}
            {{ form_errors(form.patronic) }}
            {{ form_widget(form.patronic,{'attr':{'class':'form-control'}}) }}
            {{ form_help(form.patronic) }}
        </div>
        <div class="form-group">
            {{ form_label(form.family) }}
            {{ form_errors(form.family) }}
            {{ form_widget(form.family,{'attr':{'class':'form-control'}}) }}
            {{ form_help(form.family) }}
        </div>
        <div class="form-group">
            {{ form_label(form.biograpfy) }}
            {{ form_errors(form.biograpfy) }}
            {{ form_widget(form.biograpfy) }}
            {{ form_help(form.biograpfy) }}
        </div>
        <div class="card-footer">
            <button class="btn btn btn-primary" type="submit">
                {{ button_label|default('Save') }}
            </button>
        </div>
    </div>
{{ form_end(form) }}

